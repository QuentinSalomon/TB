<UserControl x:Class="Framework.UserControlSequencer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Framework"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="ListboxErrors" BorderBrush="Red" Grid.Column="1"></ListBox>
        <StackPanel>
            <Button Content="Init" Command="{Binding Path=CommandInit}"/>
            <Button Content="Play" Command="{Binding Path=CommandPlayPlaylist}"/>
            <TextBlock Text="{Binding Xylobot.Test}"/>
        </StackPanel>
    </Grid>
</UserControl>
